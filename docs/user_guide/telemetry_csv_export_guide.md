# Real-Time Telemetry CSV Logging & Data Export

## Introduction
Analyzing exported CSV telemetry data in Python, MATLAB, and Excel.

```bash
# Run standalone desktop tracking GUI
python -m archis_tracker.main

# Run automated verification benchmark
python -m archis_tracker.main --benchmark --duration 10.0
```
